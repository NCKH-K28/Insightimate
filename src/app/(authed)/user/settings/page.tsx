import { ProfileEditForm } from '@/app/(authed)/user/profile/_components/profile-edit-form';

export default function GeneralSettingsPage() {
  return (
    <div className='max-w-2xl'>
      <ProfileEditForm />
    </div>
  );
}
