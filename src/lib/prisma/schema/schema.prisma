generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["postgresqlExtensions"]
}

datasource db {
  provider   = "postgresql"
  url        = env("DATABASE_URL")
  extensions = [vector]
}

model DebeziumSignal {
  id   String @id @default(uuid())
  type String
  data Json?

  @@map("debezium_signal")
}
