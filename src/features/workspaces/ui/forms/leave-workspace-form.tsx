type LeaveWorkspaceFormProps = {
  params: { workspaceId: string };
  onSuccess?: () => void;
  onSubmit?: () => void;
  onCancel?: () => void;
};
export const LeaveWorkspaceForm = ({}: LeaveWorkspaceFormProps) => {
  return <div>LeaveWorkspaceForm</div>;
};
